<div>
  <h1 class="font-bold text-4xl">FavoriteMovies#index</h1>
  <p>Find me in app/views/favorite_movies/index.html.erb</p>
</div>

<% if @favorites.present? %>
  <div id="movies" class="flex flex-wrap justify-between">
   <% @favorites.each do |movie| %>
    <%= render 'movies/movie', movie: movie %>
  <% end %>
  </div>
<% end %>
